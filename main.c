#include "preprocesor.h"
/*#define cos (asd, def) (asd+def)
#define CHAR_MIN (15)
#include <limits.h>*
//#  define cos 2*/
int main()
{
    //printf("nic" );
    //printf("%d", cos(2,-3));
    /*char *s=malloc(10*sizeof(char)), *s_begin=s;
    size_t len=10, len_max=10;
    for(int i=0; i<50; i++)
    {
        char *new_s=add_char_to_string(&s, s_begin, &len, &len_max, (i+'a'));
        s=new_s+(s-s_begin);
        *s='\0';
        s_begin=new_s;
        printf("%d %d %s\n", len_max, len, s_begin);
    }*/
    return start();
}
